import React from 'react';import info from './Footer';

const AboutUs = (props) => {
	return (
		<div className={props.className}>
			<p>Adress:</p>
			<p>blablablabla</p>
		</div>
	);
};

export default AboutUs;
